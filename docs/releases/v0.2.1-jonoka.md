# Release: v0.2.1-jonoka

| | |
|--|--|
| **Tag** | `v0.2.1-jonoka` |
| **Date** | 2026-08-01 |
| **Base** | `v0.2.0-jonoka`（Obsidian 插件 + 契约 + U17–U19） |
| **Theme** | V5 离线图谱 HTML 记批注（方案 A） |

## What’s new

1. **`skill-assets/graph-audit-panel.js`**  
   - Mounted in `knowledge-graph.html` via `build-graph-html.sh`  
   - Header button **记批注**  
   - Prefill `target` from selected node `source_path`  
   - Optional text selection from drawer/body  
   - **Download** open audit markdown (`source: web-viewer`)  
   - Does **not** write the vault from `file://`

2. **`audit-entry.html`**  
   - Query prefill: `?target=&anchor=&comment=&severity=`

3. **Tests**  
   - `tests/js/graph-audit-panel.test.mjs`  
   - `tests/graph-audit-panel.regression-1.sh`  
   - Evidence: `docs/v5-evidence.json`  
   - Matrix: **U20 pass**

## Still from v0.2.0

- Obsidian plugin `obsidian-plugin/llm-wiki-audit`  
- Contract v1 + `check-audit-compat.py`  
- Install guide `docs/obsidian-audit-install.md`

## Not in this tag

- Silent vault write from browser (scheme B/C)  
- Community plugin store listing  

## Install

```bash
# Skill
export CODEX_HOME="D:/CodexHome"   # if needed
cd /path/to/Jonoka/llm-wiki-skill
bash install.sh --upgrade --platform codex --target-dir "$CODEX_HOME/skills/llm-wiki"
# must run from Jonoka fork tree
```

```bash
# Graph with audit panel
bash scripts/build-graph-data.sh "<wiki_root>"
bash scripts/build-graph-html.sh "<wiki_root>"
# open wiki/knowledge-graph.html → 记批注 → move download into audit/
```

## Git

```bash
git fetch --tags
git checkout v0.2.1-jonoka
```
